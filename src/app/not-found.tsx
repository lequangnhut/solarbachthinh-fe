import CustomNotFoundPage from "@/components/commons/CustomNotFound";

const NotFoundPage = () => {
  return (
    <div className="bg-whiteBase flex h-[100vh] w-full items-center justify-center">
      <CustomNotFoundPage />
    </div>
  );
};

export default NotFoundPage;
